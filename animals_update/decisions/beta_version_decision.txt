
decisions = {
    hide_human_buildings = {
        ai_check_interval = 200
        only_rulers = yes


        potential = {
            NOT = { has_character_flag = Hide_human_building }
            Is_really_animal_trigger = yes
        }
        allow = {
            always = yes
        }
        effect = {
            set_character_flag = Hide_human_building
        }

        ai_will_do = { factor = 1 }
    }
    show_human_building = {
        ai_check_interval = 1200
        ai = no

        potential = {
            has_character_flag = Hide_human_building
            Is_really_animal_trigger = yes
            ai = no
        }
        allow = {
            always = yes
        }
        effect = {
            clr_character_flag = Hide_human_building
        }

        ai_will_do = { factor = 0 }
    }
}

targetted_decisions = {
    ####Cat decision

    cat_building_curse = {
        filter = all
        ai_target_filter = rivals

        ai_check_interval = 1000 #Not wanted for them

        from_potential = {
            ai = no
            culture = cat
            any_demesne_province = {
                has_building = ca_cat_influence_2
            }
        }
        potential = {
            always = yes
        }
        allow = {
            NOT = { is_child_of = FROM }
            NOT = { culture = cat}
            FROM = {
                prestige = 500
                piety    = 150
            }
        }
        effect = {
            prestige = -500
            piety = -150
            custom_tooltip = {
                text = cursed_text_cat
                character_event = { id = ani_plus.999 days = 7 random = 24 }
            }
        }
        ai_will_do = { factor = 0 }
    }

    cat_favor_harass = {
        filter = all
        ai_target_filter = rivals
        ai_check_interval = 1000 #Not wanted for them

        from_potential = {
            ai = no
            culture = cat
            any_demesne_province = {
                has_building = ca_cat_retinue_2
            }
            has_dlc = "Zeus" #Conclave DLC, without, impossible to hold any favors
        }
        potential = {
            always = yes
        }
        allow = {
            owes_favor_to = FROM
        }
        effect = {
            custom_tooltip = {
                text = cat_harrassment_start
                FROM = { character_event = { id = ani_plus.998 days = 7 random = 24 } }
            }
        }
        ai_will_do = { factor = 0 }
    }
}
